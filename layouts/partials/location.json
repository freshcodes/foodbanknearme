{
  "name": "{{ .Title }}",
  "address": "{{ .Params.address }}",
  "address2": {{- if .Params.address2 -}} "{{ .Params.address2 }}" {{- else -}} null {{- end -}},
  "city": "{{ .Params.city }}",
  "state": "{{ .Params.state }}",
  "postal": "{{ .Params.postal }}",
  "longitude": {{ if .Params.longitude }} "{{ .Params.longitude }}" {{ else }} null {{ end }},
  "latitude": {{ if .Params.latitude }} "{{ .Params.latitude }}" {{ else }} null {{ end }},
  "website": {{ if .Params.website }} "{{ .Params.website }}" {{ else }} null {{ end }},
  "phone": {{ if .Params.phone }} "{{ .Params.phone }}" {{ else }} null {{ end }},
  "fax": {{ if .Params.fax }} "{{ .Params.fax }}" {{ else }} null {{ end }},
  "permalink": "{{ .Permalink }}"
}
