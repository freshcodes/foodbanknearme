{{ define "response" }}
  {{- $e := sort (where (where .Site.RegularPages "Type" "locations") ".Params.partnerbank" "==" false) -}}
  {
    "state": null,
    "count" : "{{ len $e }}",
    "permalink": "{{ .Permalink }}",
    "items" : [
    {{- range $i, $r := $e -}}
    {{- if $i -}}, {{- end -}}{{- partialCached "location.json" . .Params.state .Params.city .Params.slug -}}
    {{- end -}}
    ]
  }
{{ end }}
